# 애널리틱스 아이디어

```ts
<LogInterceptor>
  <App />
</LogInterceptor>

// LogInterceptor 안에 있는 컴포넌트가 렌더링 되면 최상위 컴포넌트에 대해서만 로그 남김
// 또한 개체 구분을 위해 ref나 옵션값을 배치해서 태그를 넣으면 됨
// onClick도 감지해서 onClick 발생 시 로그 남김
```

이 아이디어가 가능한건가?
